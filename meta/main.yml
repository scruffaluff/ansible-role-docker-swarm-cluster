# Ansible role metadata file.
#
# For more information, visit
# https://docs.ansible.com/ansible/latest/user_guide/playbooks_reuse_roles.html#role-argument-validation.

---
dependencies: []

galaxy_info:
  author: Macklan Weinstein
  description: Creates a Docker Swarm cluster from an inventory file
  galaxy_tags:
    - docker
  license: MIT
  min_ansible_version: 2.1
  namespace: scruffaluff
  role_name: docker_swarm_cluster
  # For more information on available platforms, visit
  # https://galaxy.ansible.com/api/v1/platforms.
  platforms:
    - name: Fedora
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
